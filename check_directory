if [ -d "/home/bob/caleston" ] #The flag "-d" is used with the test command "[" to check if the specified file or directory exists and is a directory. If the specified file is a directory, the condition evaluates to true, and the commands inside the "then" block are executed. Otherwise, the condition evaluates to false, and the commands inside the "else" block are executed.
then
  echo "Directory exists"
else
  echo "Directory not found"
fi
